export interface ErrorPayload<T> {
    name: T;
    message: string;
    cause?: any;
}
export declare abstract class BaseError<ErrorNames extends string> extends Error {
    name: ErrorNames;
    message: string;
    cause: any;
    constructor({ name, message, cause, }: ErrorPayload<ErrorNames>);
}
