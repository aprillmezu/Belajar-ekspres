import { Schema } from 'joi';
import { QueryParser } from './query-parser';
import { RawQuery, QueryForAll } from './types';
export declare class QueryForAllParser extends QueryParser<QueryForAll> {
    schema(): Schema;
    parse(query: RawQuery): QueryForAll;
}
