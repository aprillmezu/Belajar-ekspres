import { Schema } from 'joi';
import { ParsedQuery, RawQuery } from './types';
export declare abstract class QueryParser<T extends ParsedQuery> {
    abstract schema(): Schema;
    abstract parse(query: RawQuery): T;
    make(query: RawQuery): Promise<T>;
    validate(query: RawQuery): Promise<T>;
}
